# RESERVED — Fly deploy surface for kai-chattr-api. Populated in the kai-chattr deploy slice.
# Org: personal (see secrets/dev/fly.yaml). Kept fully commented so it cannot accidentally deploy.
#
# app            = "kai-chattr-api"
# primary_region = "iad"
#
# [build]
#   dockerfile = "Dockerfile"
#
# [http_service]
#   internal_port      = 8880
#   force_https        = true
#   auto_stop_machines  = true
#   auto_start_machines = true
#   min_machines_running = 0
#
# [[mounts]]
#   source      = "kai_chattr_api_data"   # runtime owns its SQLite on a persistent volume
#   destination = "/data"
#
# [checks.health]
#   type     = "http"
#   path     = "/health"
#   interval = "15s"
#   timeout  = "2s"
